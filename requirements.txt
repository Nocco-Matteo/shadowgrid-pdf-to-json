# requirements.txt — unica sorgente di verità per le dipendenze.
# Install: pip install -r requirements.txt
#
# Sviluppo (senza GPU): così com'è (i pacchetti CUDA sono commentati).
# Target machine (RTX 4090): scommentare vllm/paddleocr/paddlepaddle (CPU).

# Il pacchetto stesso in modalità editabile
-e .

# --- Core (sempre necessari) ---
pydantic==2.13.5
pydantic-settings==2.15.0
pyyaml>=6.0.3
rapidfuzz==3.14.6

# --- OCR / image processing ---
pymupdf>=1.24
opencv-python-headless>=4.9
numpy>=1.26
pillow>=10.2

# --- UI revisione umana ---
gradio>=4.20

# --- Dev / test ---
pytest==9.1.1
pytest-cov==7.1.0
ruff==0.16.6

# --- Pesanti (CUDA) — scommentare sulla target machine ---
# Nota: vllm si tira dietro la sua torch CUDA (13.x): Paddle è volutamente la
# build CPU (`paddlepaddle`, non `-gpu`), usata solo per il layout detection;
# il riconoscimento gira su vLLM/GPU. NON installare paddlepaddle-gpu nello
# stesso venv: sovrascrive `paddle` e le versioni 2.x rompono paddlex 3.x.
# `paddleocr[doc-parser]` >= 3.3 è necessario per PaddleOCRVL.
# vllm>=0.5
# paddleocr[doc-parser]>=3.3
# paddlepaddle>=3.2
