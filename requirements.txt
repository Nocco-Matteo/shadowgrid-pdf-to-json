# requirements.txt — unica sorgente di verità per le dipendenze.
# Install: pip install -r requirements.txt
#
# Sviluppo (senza GPU): così com'è (i pacchetti CUDA sono commentati).
# Target machine (RTX 4090, CUDA 12.1+): scommentare vllm/paddlepaddle-gpu.

# Il pacchetto stesso in modalità editabile
-e .

# --- Core (sempre necessari) ---
pydantic==2.13.5
pydantic-settings==2.15.0
pyyaml>=6.0.3
rapidfuzz==3.14.6

# --- OCR / image processing ---
pymupdf>=1.24
opencv-python-headless>=4.9
numpy>=1.26
pillow>=10.2

# --- UI revisione umana ---
gradio>=4.20

# --- Dev / test ---
pytest==9.1.1
pytest-cov==7.1.0
ruff==0.16.6

# --- Pesanti (CUDA) — scommentare sulla target machine ---
# Nota: vllm si tira dietro la sua torch CUDA compatibile; installalo PRIMA
# di paddlepaddle-gpu per evitare conflitti di versione torch.
# vllm>=0.5
# paddleocr>=2.7
# paddlepaddle-gpu>=2.6
